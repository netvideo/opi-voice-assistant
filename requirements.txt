# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.35.0
accelerate>=0.20.0

# Audio processing
pyaudio>=0.2.13
sounddevice>=0.4.6
librosa>=0.10.0
soundfile>=0.12.0
webrtcvad>=2.0.10

# RKLLM for RK3588
# 注意: RKLLM需要从Rockchip官方仓库手动安装
# 详见 scripts/install.sh

# ASR & TTS
datasets>=2.14.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Cloud API support
requests>=2.31.0
urllib3>=2.0.0

# Utils
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0

# Optional: model downloading
modelscope>=1.9.0

# Optional: for better performance
# bitsandbytes  # 4-bit量化 (RK3588上可能不需要)
